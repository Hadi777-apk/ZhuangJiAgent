<UserControl x:Class="DevEnvInit.App.Views.SoftwareSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource CardBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="软件清单" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="从真实 config.json 加载启用软件，按安装策略给出默认选择；需人工确认的软件不会默认勾选。" Margin="0,12,0,0" Foreground="{StaticResource BrushMutedText}" TextWrapping="Wrap" />
            </StackPanel>

            <DockPanel Grid.Row="1" Margin="0,18,0,14">
                <Button DockPanel.Dock="Right" Content="加载清单" Padding="18,8" Command="{Binding LoadPackagesCommand}" />
                <TextBlock Text="{Binding Summary}" VerticalAlignment="Center" Foreground="{StaticResource BrushMutedText}" TextWrapping="Wrap" />
            </DockPanel>

            <DataGrid Grid.Row="2" ItemsSource="{Binding Packages}" AutoGenerateColumns="False" HeadersVisibility="Column" Background="{StaticResource BrushSurfaceAlt}" Foreground="{StaticResource BrushText}" BorderBrush="{StaticResource BrushBorder}" GridLinesVisibility="None">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="选择" Binding="{Binding IsSelected}" Width="70" />
                    <DataGridTextColumn Header="软件" Binding="{Binding Name}" Width="180" IsReadOnly="True" />
                    <DataGridTextColumn Header="安装包" Binding="{Binding Installer}" Width="180" IsReadOnly="True" />
                    <DataGridTextColumn Header="版本要求" Binding="{Binding RequiredVersion}" Width="130" IsReadOnly="True" />
                    <DataGridTextColumn Header="安装策略" Binding="{Binding PolicyText}" Width="150" IsReadOnly="True" />
                    <DataGridCheckBoxColumn Header="可自动选择" Binding="{Binding IsEnabled}" Width="110" IsReadOnly="True" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
