<UserControl x:Class="DevEnvInit.App.Views.DetectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource CardBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="环境检测" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="执行只读检测：管理员权限、系统信息、网络状态、已配置路径和本地安装包可用性。" Margin="0,12,0,0" Foreground="{StaticResource BrushMutedText}" TextWrapping="Wrap" />
            </StackPanel>

            <DockPanel Grid.Row="1" Margin="0,18,0,14">
                <Button DockPanel.Dock="Right" Content="开始检测" Padding="18,8" Command="{Binding RunDetectionCommand}" />
                <TextBlock Text="{Binding Summary}" VerticalAlignment="Center" Foreground="{StaticResource BrushMutedText}" TextWrapping="Wrap" />
            </DockPanel>

            <DataGrid Grid.Row="2" ItemsSource="{Binding Results}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="{StaticResource BrushSurfaceAlt}" Foreground="{StaticResource BrushText}" BorderBrush="{StaticResource BrushBorder}" GridLinesVisibility="None">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="软件" Binding="{Binding PackageName}" Width="180" />
                    <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="160" />
                    <DataGridTextColumn Header="检测路径" Binding="{Binding DetectedPath}" Width="260" />
                    <DataGridTextColumn Header="说明" Binding="{Binding Message}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
