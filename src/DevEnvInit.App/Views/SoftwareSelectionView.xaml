<UserControl x:Class="DevEnvInit.App.Views.SoftwareSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource CardBorderStyle}">
        <StackPanel>
            <TextBlock Text="软件清单" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="后续阶段加载真实 config.json，并按缺失、已安装、可选和需人工确认分组。" Margin="0,12,0,0" Foreground="{StaticResource BrushMutedText}" TextWrapping="Wrap" />
        </StackPanel>
    </Border>
</UserControl>
