<UserControl x:Class="DevEnvInit.App.Views.PathConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource CardBorderStyle}">
        <StackPanel>
            <TextBlock Text="路径配置" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="后续阶段配置安装根目录、下载缓存目录和临时解压目录；本页不放命令预览区域。" Margin="0,12,0,0" Foreground="{StaticResource BrushMutedText}" TextWrapping="Wrap" />
        </StackPanel>
    </Border>
</UserControl>
