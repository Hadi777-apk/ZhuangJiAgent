<UserControl x:Class="DevEnvInit.App.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource CardBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="180" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="完成报告" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="生成并展示安装结果报告，包含成功、跳过、失败记录及日志路径。" Margin="0,12,0,0" Foreground="{StaticResource BrushMutedText}" TextWrapping="Wrap" />
            </StackPanel>

            <DockPanel Grid.Row="1" Margin="0,18,0,14">
                <Button DockPanel.Dock="Right" Content="生成报告" Padding="18,8" Command="{Binding GenerateReportCommand}" />
                <TextBlock Text="{Binding Summary}" VerticalAlignment="Center" Foreground="{StaticResource BrushMutedText}" TextWrapping="Wrap" />
            </DockPanel>

            <DataGrid Grid.Row="2" ItemsSource="{Binding ReportItems}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="{StaticResource BrushSurfaceAlt}" Foreground="{StaticResource BrushText}" BorderBrush="{StaticResource BrushBorder}" GridLinesVisibility="None">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="软件" Binding="{Binding PackageName}" Width="200" />
                    <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="120" />
                    <DataGridTextColumn Header="说明" Binding="{Binding Message}" Width="*" />
                    <DataGridTextColumn Header="日志路径" Binding="{Binding LogPath}" Width="200" />
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Row="3" Background="{StaticResource BrushSurfaceAlt}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Padding="12">
                <ScrollViewer>
                    <TextBlock Text="{Binding ReportContent}" FontFamily="Consolas" FontSize="12" Foreground="{StaticResource BrushMutedText}" TextWrapping="Wrap" />
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</UserControl>
