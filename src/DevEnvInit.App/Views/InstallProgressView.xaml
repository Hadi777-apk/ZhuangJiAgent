<UserControl x:Class="DevEnvInit.App.Views.InstallProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource CardBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="180" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="执行安装" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="Dry-run 模式：模拟安装流程，不执行真实安装器。后续阶段接入真实安装执行。" Margin="0,12,0,0" Foreground="{StaticResource BrushMutedText}" TextWrapping="Wrap" />
            </StackPanel>

            <DockPanel Grid.Row="1" Margin="0,18,0,12">
                <Button DockPanel.Dock="Right" Content="启动 Dry-Run" Padding="18,8" Command="{Binding StartInstallCommand}" />
                <TextBlock Text="{Binding Summary}" VerticalAlignment="Center" Foreground="{StaticResource BrushMutedText}" TextWrapping="Wrap" />
            </DockPanel>

            <ProgressBar Grid.Row="2" Value="{Binding ProgressPercent}" Height="8" Margin="0,0,0,14" Foreground="{StaticResource BrushAccent}" Background="{StaticResource BrushSurfaceAlt}" BorderBrush="{StaticResource BrushBorder}" />

            <DataGrid Grid.Row="3" ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="{StaticResource BrushSurfaceAlt}" Foreground="{StaticResource BrushText}" BorderBrush="{StaticResource BrushBorder}" GridLinesVisibility="None" Margin="0,0,0,10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="软件" Binding="{Binding PackageName}" Width="240" />
                    <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="140" />
                    <DataGridTextColumn Header="消息" Binding="{Binding LogMessage}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Row="4" Background="{StaticResource BrushSurfaceAlt}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Padding="12">
                <ScrollViewer>
                    <ItemsControl ItemsSource="{Binding LogLines}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="12" Foreground="{StaticResource BrushMutedText}" TextWrapping="Wrap" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</UserControl>
